<div class="accordion" id="accordionExample">
    <div class="accordion-item">
      <h2 class="accordion-header">
        <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseTwo" aria-expanded="false" aria-controls="collapseTwo">
          Add Employees to review list
        </button>
      </h2>
      <div id="collapseTwo" class="accordion-collapse collapse" data-bs-parent="#accordionExample">
        <div class="accordion-body">
          <% if(non_auth_list.length === 0) { %>
            <strong> The List is Empty </strong>
          <% } else { %>
            <ul class="list-group">
              <% for(listEmployee of non_auth_list) { %>
                <li class="list-group-item">
                  <input type="checkbox" class="custom-control-input" id="<%= listEmployee.email %>" name="auth_list" value="<%= listEmployee._id %>">
                  <label class="custom-control-label" for="checkbox1"><%= listEmployee.email %></label>
                </li>
              <% } %>
            </ul>
            <div class="form-group mt-3" style="text-align: center;">
              <button type="submit" class="btn btn-secondary btn-block" formaction="/dashboard/admin/add_to_list/<%= employee._id %>">Add Employees to list</button>
            </div>
          <% } %>
        </div>
      </div>
    </div>
  </div>